# 基于timm库的PyTorch通用图像分类框架

<div align="center">

![Python Version](https://img.shields.io/badge/python-3.6+-blue.svg)
![PyTorch Version](https://img.shields.io/badge/pytorch-1.2.0+-orange.svg)
![License](https://img.shields.io/badge/license-MIT-green.svg)

一个功能完善的图像分类深度学习框架,支持timm中多种主流CNN和Transformer架构,提供完整的训练、评估和推理流程。

[特性](#-特性) • [快速开始](#-快速开始) • [模型支持](#-支持的模型) • [评估指标](#-评估指标) • [文档](#-详细文档)

</div>

---

## 📋 目录

- [特性](#-特性)
- [环境要求](#-环境要求)
- [安装](#-安装)
- [快速开始](#-快速开始)
- [支持的模型](#-支持的模型)
- [项目结构](#-项目结构)
- [详细文档](#-详细文档)
- [评估指标](#-评估指标)
- [常见问题](#-常见问题)
- [更新日志](#-更新日志)
- [许可证](#-许可证)

---

## ✨ 特性

- 🚀 **多模型支持**: 集成15+种主流架构(ResNet、VGG、EfficientNet、ViT、Swin Transformer等)
- 🎯 **完整的训练流程**: 两阶段训练策略(冻结+解冻),自适应学习率调整
- 📊 **丰富的评估指标**: 准确率、Precision、Recall、F1、AUC、ROC曲线、混淆矩阵等
- 🔄 **类别不平衡处理**: 支持Focal Loss、类别加权、平衡采样等策略
- 📈 **可视化分析**: 自动生成训练曲线、ROC/PR曲线、混淆矩阵等可视化报告
- 💻 **分布式训练**: 支持DP和DDP多GPU训练模式
- 🎨 **数据增强**: 内置多种数据增强策略,提升模型泛化能力
- 📦 **即插即用**: 简洁的配置,最少3个参数即可开始训练

---

## 🔧 环境要求

- **Python**: 3.6+ (推荐3.8+)
- **PyTorch**: 1.2.0+ (推荐2.0+)
- **CUDA**: 可选,用于GPU加速训练
- **操作系统**: Windows / Linux / macOS

---

## 📦 安装

### 1. 克隆仓库

```bash
git clone https://github.com/yourusername/classification-pytorch.git
cd classification-pytorch
```

### 2. 创建虚拟环境(推荐)

```bash
# 使用conda
conda create -n cls python=3.8
conda activate cls

# 或使用venv
python -m venv venv
source venv/bin/activate  # Linux/macOS
venv\Scripts\activate     # Windows
```

### 3. 安装依赖

```bash
# 标准安装
pip install -r requirements.txt

# 如果使用较新的Python版本(3.11+),依赖会自动适配
```

**核心依赖:**
- `torch>=1.2.0` - PyTorch深度学习框架
- `torchvision>=0.4.0` - 图像处理工具
- `timm` - 预训练模型库(1000+模型)
- `scikit-learn>=1.0.0` - 评估指标计算
- `matplotlib>=3.1.2` - 可视化
- `opencv-python>=4.1.2` - 图像处理
- `pandas>=2.0.0` - 数据处理
- `seaborn>=0.13.0` - 高级可视化

---

## 🚀 快速开始

### 1. 准备数据集

按以下结构组织你的数据:

```
datasets/
├── train/
│   ├── class_0/
│   │   ├── img1.jpg
│   │   └── img2.jpg
│   ├── class_1/
│   └── class_2/
└── test/
    ├── class_0/
    ├── class_1/
    └── class_2/
```

### 2. 生成标注文件

```bash
python txt_annotation.py
```

这将生成 `cls_train.txt` 和 `cls_test.txt` 标注文件,以及 `model_data/cls_classes.txt` 类别文件。

### 3. 配置模型参数

编辑 `train.py` 或 `train_trimm.py` 中的关键参数:

```python
# 模型选择
backbone = 'inception_resnet_v2'  # 可选: resnet50, efficientnet_b0, vit_b_16等
input_shape = [299, 299]          # 输入图像尺寸

# 训练参数
Init_Epoch = 0
Freeze_Epoch = 50                 # 冻结训练轮数
Epoch = 300                       # 总训练轮数
batch_size = 32
```

### 4. 开始训练

```bash
# 标准训练
python train.py

# 使用timm模型库训练(推荐)
python train_trimm.py

# 多GPU训练(Linux)
CUDA_VISIBLE_DEVICES=0,1 python train.py
```

### 5. 模型评估

```bash
python eval.py
```

评估结果将保存在 `metrics_out/` 目录,包括:
- 📊 混淆矩阵 (`confusion_matrix.png`)
- 📈 ROC/PR曲线 (`roc_curves.png`, `pr_curves.png`)
- 📉 指标对比图 (`metrics_comparison_chart.png`)
- 📝 完整分类报告 (`classification_report.txt`)

### 6. 模型推理

```bash
# 单张图片预测
python predict.py
# 然后输入图片路径

# 批量预测
python Predict_All_Precision_Calculation.py
```

---

## 🏗️ 支持的模型

### 经典CNN架构

| 模型 | Input Size | 参数量 | 特点 |
|------|-----------|--------|------|
| **ResNet** (18/34/50/101/152) | 224×224 | 11M-60M | 残差连接,深层网络训练稳定 |
| **VGG** (11/13/16 + BN变体) | 224×224 | 128M-143M | 经典架构,特征提取强 |
| **MobileNetV2** | 224×224 | 3.5M | 轻量级,适合移动端部署 |
| **DenseNet** (121/161/169/201) | 224×224 | 7M-44M | 密集连接,参数效率高 |
| **EfficientNet** (B0-B7) | 224×224+ | 5M-66M | SOTA准确率,效率优化 |
| **Inception系列** | 299×299 | 23M-56M | 多尺度特征融合 |
| **Xception** | 299×299 | 23M | 深度可分离卷积 |
| **InceptionResNetV2** | 299×299 | 56M | Inception+残差,高准确率 |

### Transformer架构

| 模型 | Input Size | 参数量 | 特点 |
|------|-----------|--------|------|
| **ViT-B/16** | 224×224 | 86M | Vision Transformer,大数据集表现优异 |
| **Swin Transformer** (Tiny/Small/Base) | 224×224 | 28M-88M | 层次化Transformer,平衡性能和效率 |

### 使用timm库扩展

通过 `train_trimm.py` 可以使用 **1000+种预训练模型**:

```python
# 示例:使用EfficientNet-B3
model = timm.create_model('efficientnet_b3', pretrained=True, num_classes=num_classes)
```

---

## 📁 项目结构

```
classification-pytorch/
├── datasets/              # 数据集目录
│   ├── train/            # 训练集
│   └── test/             # 测试集
├── model_data/           # 模型配置文件
│   └── cls_classes.txt   # 类别标签文件
├── models/               # 保存的模型权重
├── logs/                 # 训练日志和检查点
├── metrics_out/          # 评估结果输出
├── nets/                 # 模型架构实现
│   ├── resnet.py
│   ├── vgg.py
│   ├── mobilenetv2.py
│   ├── vision_transformer.py
│   ├── swin_transformer.py
│   └── ...
├── utils/                # 工具函数
│   ├── dataloader.py     # 数据加载器
│   ├── utils_fit.py      # 训练流程
│   ├── utils_metrics.py  # 评估指标
│   ├── callbacks.py      # 回调函数
│   └── early_stopping.py # 早停和检查点
├── train.py              # 标准训练脚本
├── train_trimm.py        # timm模型训练脚本
├── eval.py               # 模型评估脚本
├── predict.py            # 单张图片推理
├── classification.py     # 推理引擎
├── txt_annotation.py     # 数据集标注生成
└── requirements.txt      # 依赖清单
```

---

## 📚 详细文档

### 配置同步说明

**重要**: 以下参数必须在多个文件中保持一致:

| 参数 | train.py | classification.py | 说明 |
|------|----------|-------------------|------|
| `backbone` | 第66行 | 第39行 | 模型架构名称 |
| `input_shape` | 第55行 | 第28行 | 输入图像尺寸 |
| `model_path` | 第88行 | 第23行 | 权重文件路径 |
| `classes_path` | 第51行 | 第24行 | 类别文件路径 |

### 训练策略说明

#### 两阶段训练

1. **阶段1 - 冻结主干网络** (0-50轮)
   - 仅训练分类头
   - 大批量训练 (batch_size=128)
   - 学习率: 1e-3
   - 目的: 快速适应新数据集

2. **阶段2 - 解冻全部网络** (50-300轮)
   - 训练所有层
   - 小批量训练 (batch_size=32)
   - 学习率: 1e-4 (余弦衰减)
   - 目的: 精细调优

#### 学习率调度

支持三种策略:
- `cos`: 余弦退火 (推荐)
- `step`: 阶梯式衰减
- `linear`: 线性衰减

#### 优化器选择

```python
optimizer_type = "adam"  # 或 "sgd"
momentum = 0.9           # SGD动量(仅SGD)
weight_decay = 0         # L2正则化
```

### 数据增强

支持多种增强策略:
- 随机裁剪和缩放
- 随机水平/垂直翻转
- 颜色抖动(亮度、对比度、饱和度)
- 随机旋转
- Cutout/RandomErasing

配置位置: `utils/dataloader.py`

### 类别不平衡处理

1. **Focal Loss**: 自动降低易分类样本的权重
2. **类别加权**: 根据样本数量自动计算权重
3. **平衡采样**: 保证每个batch类别分布均衡

### 混合精度训练

```python
# train.py中启用
fp16 = True  # 减少50%显存,加速1.5-2倍
```

要求: PyTorch >= 1.7.1

---

## 📊 评估指标

### 基础指标

- **Top-1 Accuracy**: 最高预测准确率
- **Top-5 Accuracy**: 前5预测准确率
- **Precision**: 精确率 (TP / (TP + FP))
- **Recall**: 召回率 (TP / (TP + FN))
- **F1-Score**: 精确率和召回率的调和平均

### 高级指标

- **AUC**: 受试者工作特征曲线下面积
  - Per-class AUC
  - Macro-AUC (类别平均)
  - Micro-AUC (样本平均)
- **Specificity**: 特异性 (TN / (TN + FP))
- **Sensitivity**: 敏感性 (等同于Recall)
- **Bootstrap 95% 置信区间**: 1000次重采样统计

### 可视化输出

运行 `eval.py` 后,将在 `metrics_out/` 目录生成:

```
metrics_out/
├── confusion_matrix.png              # 混淆矩阵热力图
├── confusion_matrix.csv              # 混淆矩阵CSV
├── roc_curves.png                    # ROC曲线(含Macro/Micro平均)
├── pr_curves.png                     # PR曲线(含Macro/Micro平均)
├── confidence_intervals.png          # 6个指标的95%置信区间
├── metrics_comparison_chart.png      # 指标对比柱状图
├── Precision.png                     # 各类别精确率
├── Recall.png                        # 各类别召回率
└── classification_report.txt         # 完整分类报告(文本)
```

---

## ❓ 常见问题

### 1. 形状不匹配错误

**问题**: `RuntimeError: size mismatch`

**解决方案**:
- 检查 `input_shape` 是否符合模型要求(如Inception系列需要299×299)
- 确认 `cls_classes.txt` 中的类别数量与模型匹配
- 验证预训练权重与主干架构对应

### 2. 显存不足 (CUDA Out of Memory)

**解决方案**:
```python
# 方法1: 减小批量大小
batch_size = 16  # 或更小

# 方法2: 启用混合精度训练
fp16 = True

# 方法3: 使用轻量级模型
backbone = 'mobilenetv2'
```

### 3. 训练中断后恢复

```python
# 在train.py中设置
model_path = 'logs/ep050-loss0.123-val_loss0.456.pth'  # 检查点路径
Init_Epoch = 50  # 从第50轮恢复
Freeze_Epoch = 50  # 已完成冻结训练
```

### 4. 如何使用自定义数据集?

1. 按 [快速开始](#快速开始) 中的目录结构组织数据
2. 运行 `txt_annotation.py` 生成标注
3. 修改 `train.py` 中的 `classes_path` 和类别数量
4. 开始训练

### 5. 多GPU训练支持吗?

**支持**, 但目前仅在Linux系统测试:

```bash
# DataParallel模式
CUDA_VISIBLE_DEVICES=0,1 python train.py

# DistributedDataParallel模式(推荐)
CUDA_VISIBLE_DEVICES=0,1 python -m torch.distributed.launch \
    --nproc_per_node=2 train.py
```

### 6. 如何导出为ONNX?

```python
import torch

model = ...  # 加载你的模型
model.eval()

dummy_input = torch.randn(1, 3, 224, 224)
torch.onnx.export(
    model,
    dummy_input,
    "model.onnx",
    opset_version=11,
    input_names=['input'],
    output_names=['output']
)
```

## 📄 许可证

本项目采用 [MIT License](LICENSE) 开源协议。

---

## 🙏 致谢

- [PyTorch](https://pytorch.org/) - 深度学习框架
- [timm](https://github.com/huggingface/pytorch-image-models) - 预训练模型库
- [scikit-learn](https://scikit-learn.org/) - 评估指标工具

---


<div align="center">

**如果这个项目对你有帮助,请给一个⭐Star支持一下!**

Made with ❤️ by [wuxb55]

</div>
